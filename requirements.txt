# Core runtime — everything here is free and works offline.
playwright>=1.47
Pillow>=10.0          # screenshot downscaling: 1440px -> 1024px is ~half the image tokens
fastapi>=0.115
uvicorn[standard]>=0.30   # [standard] is not optional: the UI is a WebSocket.
                          # Plain uvicorn has no WS implementation and the page
                          # never connects ("Unsupported upgrade request").
httpx>=0.27
PyYAML>=6.0
jinja2>=3.1

# The decision engine. Pulls in torch (~2.5 GB), so it is installed separately
# in the quickstart. VibeBot degrades to a heuristic decider without it.
# laya>=0.3.3
